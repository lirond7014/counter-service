apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: counter-service

resources:
  - deployment.yml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - serviceaccount.yaml
  - postgres.yaml

commonLabels:
  app: counter-service