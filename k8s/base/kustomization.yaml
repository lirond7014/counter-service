apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: counter-service

resources:
  - pdb-postgres.yml
  - pdb.yml
  - deployment.yml
  - service.yml
  - configmap.yml
  - postgres.yaml
  - token_job.yml
  - hpa-backend.yml
  - hpa-frontend.yml

commonLabels:
  app: counter-service
